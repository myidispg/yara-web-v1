# yara-web-v1


## First Time Setup

After cloning the repository, run:

```bash
chmod +x setup.sh
./setup.sh

Then start servers
# Terminal 1 - Backend
cd backend
python manage.py runserver

# Terminal 2 - Frontend  
cd frontend
npm run dev